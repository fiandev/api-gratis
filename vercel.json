{
    "buildCommand": "bun run build && bun run start",
    "builds": [
        {
            "src": "public/**",
            "use": "@vercel/static"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "./dist/server.js"
        }
    ]
}
